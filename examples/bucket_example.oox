class Main {
    
    static void main(int b, int c)
    {
        int total_count := 0;
        int i := 0;
        int a := 0;

        while(i < b){
            i := i + 1;
            if (i == 1123233447 && c == 12) {
                a := 0;
            }
        }
        // 55034342
        // 1123233500
        if (i > 200) {
            if (c == 12){
                assert(c == 14);
            }
        }

        // assert(false);

        // assert(a == i);
    }
}